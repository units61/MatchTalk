module.exports = {
  // Print width - maksimum satır uzunluğu
  printWidth: 100,
  
  // Tab width - girinti genişliği
  tabWidth: 2,
  
  // Tabs yerine spaces kullan
  useTabs: false,
  
  // Semicolon kullan
  semi: true,
  
  // Single quotes kullan
  singleQuote: true,
  
  // Trailing commas - ES5 uyumlu (objects, arrays, vb.)
  trailingComma: 'all',
  
  // Bracket spacing - { foo: bar } vs {foo: bar}
  bracketSpacing: true,
  
  // JSX bracket same line
  bracketSameLine: false,
  
  // Arrow function parentheses - (x) => x vs x => x
  arrowParens: 'avoid',
  
  // End of line - lf, crlf, cr, auto
  endOfLine: 'lf',
};


